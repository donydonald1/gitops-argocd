---
# =============================================================================
# RBAC for GitHub Actions Runners
# =============================================================================
# Grants runners permission to restart deployments in designbuy namespaces
# for CI/CD deploy steps (kubectl rollout restart)
# =============================================================================

# ServiceAccount used by runner pods
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gha-runner-deploy
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: github-actions-runner
    app.kubernetes.io/instance: {{ .Release.Name }}

---
# ClusterRole with minimal permissions to restart deployments
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gha-runner-deploy
  labels:
    app.kubernetes.io/name: github-actions-runner
    app.kubernetes.io/instance: {{ .Release.Name }}
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "patch"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get"]

---
# Role for Docker Buildx builder (Kubernetes driver) in the runner namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: buildx-builder
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: github-actions-runner
    app.kubernetes.io/instance: {{ .Release.Name }}
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "create", "update", "patch", "delete", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods", "pods/log", "pods/exec", "services", "configmaps", "secrets"]
    verbs: ["get", "create", "update", "patch", "delete", "list", "watch"]

---
# Bind buildx-builder role to both runner service accounts
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: buildx-builder
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: github-actions-runner
    app.kubernetes.io/instance: {{ .Release.Name }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: buildx-builder
subjects:
  - kind: ServiceAccount
    name: gha-runner-deploy
    namespace: {{ .Release.Namespace }}
  - kind: ServiceAccount
    name: techsecom-k8s-runners-gha-rs-controller-kube-mode
    namespace: {{ .Release.Namespace }}

---
# Bind to designbuy namespaces
{{- $namespaces := list "designbuy-gateway" "designbuy-node" "designbuy-java" "designbuy-python" "designbuy-apps" "designbuy-data" }}
{{- range $ns := $namespaces }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: gha-runner-deploy
  namespace: {{ $ns }}
  labels:
    app.kubernetes.io/name: github-actions-runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: gha-runner-deploy
subjects:
  - kind: ServiceAccount
    name: gha-runner-deploy
    namespace: {{ $.Release.Namespace }}
---
{{- end }}
