apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: awx-user-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: awx-user-secret
    creationPolicy: Owner
    template:
      metadata:
        labels:
          cnpg.io/reload: ""
  data:
    - secretKey: username
      remoteRef:
        key: awx
        property: dbUsername
    - secretKey: password
      remoteRef:
        key: awx
        property: dbPassword
