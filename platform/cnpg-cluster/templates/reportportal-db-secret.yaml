apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
    name: reportportal-user-secret
spec:
    refreshInterval: 1h
    secretStoreRef:
        kind: ClusterSecretStore
        name: vault
    target:
        name: reportportal-user-secret
        creationPolicy: Owner
        template:
            metadata:
                labels:
                    cnpg.io/reload: ""
    data:
      - secretKey: username
        remoteRef:
            key: secret/reportportal-postgres-auth/
            property: username
      - secretKey: password
        remoteRef:
            key: secret/reportportal-postgres-auth/
            property: password
