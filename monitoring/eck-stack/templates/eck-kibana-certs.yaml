{{- if and .Values.certificates.enabled .Values.certificates.kibana.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.certificates.kibana.secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kibana
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: tls-certificate
spec:
  isCA: false
  commonName: kibana
  secretName: {{ .Values.certificates.kibana.secretName }}
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days
  subject:
    organizations:
      - {{ .Values.certificates.organization }}
  privateKey:
    algorithm: {{ .Values.certificates.kibana.privateKey.algorithm }}
    encoding: PKCS1
    size: {{ .Values.certificates.kibana.privateKey.size }}
  usages:
    - server auth
    - client auth
  dnsNames:
    {{- range .Values.certificates.kibana.dnsNames }}
    - {{ . }}
    {{- end }}
    {{- range .Values.certificates.kibana.externalDnsNames }}
    - {{ . }}
    {{- end }}
  issuerRef:
    kind: Issuer
    name: {{ .Values.certificates.issuer.issuerName }}
{{- end }}
