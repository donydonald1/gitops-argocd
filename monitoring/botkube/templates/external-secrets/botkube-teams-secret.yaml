{{- if .Values.eso.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: botkube-teams-secret
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: botkube-teams-secret
    creationPolicy: Owner
  data:
    - secretKey: BOTKUBE_COMMUNICATIONS_TEAMS-GROUP_TEAMS_APPID
      remoteRef:
        key: botkube
        property: teamsAppId
    - secretKey: BOTKUBE_COMMUNICATIONS_TEAMS-GROUP_TEAMS_APPPASSWORD
      remoteRef:
        key: botkube
        property: teamsAppPassword
{{- end }}
